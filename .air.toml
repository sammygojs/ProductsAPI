# .air.toml

root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o tmp/main ./cmd"
  bin = "tmp/main"
  full_bin = "tmp/main"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor"]
  log = "air.log"

[log]
  time = true
